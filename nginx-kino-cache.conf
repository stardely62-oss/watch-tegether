# Included from conf.d (http context)
# Bounded poster cache for /api/img (do not redeclare gzip — Ubuntu nginx.conf already enables it)

proxy_cache_path /var/cache/nginx/kino-img levels=1:2 keys_zone=kino_img:10m
                 max_size=64m inactive=7d use_temp_path=off;
